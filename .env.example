# Environment Variables Template (RoktoKonika)
# Copy this file to `.env` when connecting to a real backend server or external APIs.

# Base Backend API URL (Laravel / Node.js / Express)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# App Mode
VITE_APP_ENV=development
